export function App() {
  return (
    <main className="app-shell">
      <section aria-labelledby="app-title" className="welcome-panel">
        <p className="eyebrow">Project foundation</p>
        <h1 id="app-title">MF-JSON Visualizer</h1>
        <p>
          A workspace for exploring moving features across synchronized spatial
          and temporal views.
        </p>
      </section>
    </main>
  )
}
